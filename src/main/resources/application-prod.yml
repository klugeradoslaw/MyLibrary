spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mylibrary
    username: root
    password: root
  liquibase:
    contexts: prod
  jpa:
    hibernate:
      ddl-auto: validate